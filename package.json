{
  "name": "mongo-postgres-etl-pipeline",
  "version": "1.0.0",
  "description": "Production ETL pipeline from MongoDB to PostgreSQL",
  "main": "src/index.ts",
  "scripts": {
    "start": "ts-node src/index.ts",
    "etl:start": "ts-node src/etl.ts",
    "db:truncate": "ts-node src/scripts/truncate.ts",
    "build": "tsc",
    "generate": "prisma generate",
    "seed:mongo": "ts-node src/seed/seed-mongo.ts"
  },
  "dependencies": {
    "@prisma/client": "latest",
    "dotenv": "^16.0.0",
    "mongoose": "^8.0.0",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "@prisma/config": "^7.8.0",
    "@types/node": "^20.0.0",
    "prisma": "latest",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
